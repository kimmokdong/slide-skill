# 🏆 프롬프트 & 개발 히스토리

## 📌 [2026-06-19] 로컬 프로젝트 스킬 우선 적용 및 글로벌 자동 역동기화 차단 (17차 보완)
- **현상 및 필요성**:
  - `text-to-slide-architect` 스킬이 로컬 프로젝트 폴더(`.agents/skills/text-to-slide-architect`)로 복사됨에 따라 에이전트는 로컬 스킬을 우선 사용함.
  - 이 로컬 스킬의 템플릿, 스크립트 등을 커스텀 수정하는 과정에서 검증되지 않은 변경 사항이 사용자 동의 없이 내부 전역 스킬 폴더(`C:\Users\user\.gemini\config\skills`)나 옵시디언 금고(`g:\내 드라이브\...`)로 자동 역동기화되어 덮어써지는 리스크를 차단해야 함.
- **합의 및 대응 규칙**:
  - 로컬 스킬 수정 사항은 사용자의 명시적인 수동 최신화 지시가 있을 때까지 전역 스킬 폴더나 옵시디언 금고로 자동 덮어쓰기(역동기화)를 진행하지 않고 본 프로젝트에만 격리 반영하기로 확정함.

## 📌 [2026-06-18] 화면 전환 모션 클래스명 매핑 불일치 해결 및 실시간 모션 블러 효과 연동 (16차 보완)
- **현상 및 문제**:
  - 화면 전환 모션 드롭다운 선택 메뉴의 옵션 값(`friendly`, `cyber`, `classic`, `editorial`, `chalkboard`)과 CSS에 선언된 화면 전환 클래스 명칭(`.motion-bounce`, `.motion-scan`, `.motion-fade`, `.motion-flip`, `.motion-nature`)이 서로 맞지 않아, 모션이 전혀 적용되지 않고 기본 트랜지션만 연출되는 매핑 누락 현상이 발생함.
  - 미래 테크 고속 스캔(`cyber`) 모션 전환 도중 화면이 일시적으로 흐려지는 모션 블러 효과(`.moving` 하의 `filter: blur`)가 기획되어 있었으나, 자바스크립트 측에서 슬라이드 이동 시 `slide-track`에 `moving` 클래스를 토글해주는 타이밍 제어가 구현되어 있지 않아 작동하지 않았음.
- **해결 및 개선 패턴**:
  - `theme_demo.html`의 CSS 내 모션 클래스명들을 드롭다운 값과 정합되도록 `.motion-friendly`, `.motion-cyber`, `.motion-classic`, `.motion-editorial`, `.motion-chalkboard`로 수정함.
  - `updateSlidePosition` 함수에서 슬라이드 이동이 시작될 때 `slide-track`에 `moving` 클래스를 즉시 더하고, `transitionend` 이벤트 수신기를 1회성으로 걸어 트랜지션 완료 타이밍에 `moving` 클래스를 확실히 걷어내도록 동적 클래스 제어 코드를 추가해 사이버 테크 스캔 블러가 역동적으로 렌더링되도록 보장함.

## 📌 [2026-06-18] 무작위 테마 셔플(Random Shuffle) 기능 및 미려한 펄스 칩 추가 (15차 보완)
- **현상 및 필요성**:
  - 사전 정의된 5가지 칩셋 프리셋 외에도 동적 테마의 수많은 디자인 조합(컬러 6종, 카드 6종, 아이콘 5종, 폰트 5종, 패턴 6종, 데코 6종, 강조 5종, 하단바 5종, 모션 5종 등)을 완전히 무작위로 조합하여 예상치 못한 레이아웃 뒤틀림이나 명암 대비 결함을 스트레스 테스트할 수 있는 셔플(Shuffle) 도구가 필요했음.
- **해결 및 개선 패턴**:
  - `theme_demo.html` 마크업의 프리셋 영역 맨 뒤에 그라데이션 및 펄스(Pulse) 광원 애니메이션이 흐르는 `🎲 무작위 셔플` 칩을 배치함.
  - 자바스크립트 내 `applyRandomPreset()` 함수를 구현하여, 명도 대비 가독성이 유지되는 8가지 조화로운 60-30-10 컬러 팔레트 군(큐트, 테크, 모던, 매거진, 레트로, 오가닉, 로즈, 선셋) 중 하나를 무작위 선택하고, 그 외 카드/아이콘/폰트/패턴/강조/하단바/모션/장식 레이어(40% 개별 확률 토글) 등을 완전히 독립 무작위로 선별 적용한 뒤, 실시간 폰트 오토핏(Auto-Fit)을 자동 리트리거하도록 정합함.

## 📌 [2026-06-18] 탭/글꼴 변경 시 Auto-Fit 재계산 누락 및 슬라이드 제목 UI 겹침 버그 해결 (14차 보완)
- **현상 및 문제**:
  - 데모 시뮬레이터에서 탭 전환(`switchLayout`)이나 폰트 설정 변경(`updateFont`) 시점에 글자 크기 자동 조절(Auto-Fit)이 다시 호출되지 않아, 폰트 종류나 레이아웃이 바뀔 때 글자 크기가 실시간으로 재피팅되지 않고 이전 레이아웃 크기로 방치되거나 깨지는 현상 발생.
  - 슬라이드 제목(`.slide-title`)의 폰트를 축소해 오버플로우를 막는 Auto-Fit 내부 선택자가 `.slide-header h2`로 잘못 지정되어 있었으나, 실제 마크업에서는 `.slide-header` 밖에 `.slide-title`로 독립되어 있어 제목 폰트 축소 로직이 무력화되었음.
  - `title`, `hero` 등의 `center-layout`으로 전환 시, 상단 제목(`.slide-title`)이 숨겨지지 않아 렌더링된 텍스트와 지저분하게 겹쳐 보이는 UI 깨짐 현상 발생.
- **해결 및 개선 패턴**:
  - `switchLayout` 함수 내부에서 `center-layout` 계열 전환 시 `.slide-header`뿐 아니라 `.slide-title`도 함께 `display: none` 처리하고, 일반 레이아웃 복원 시 `display: block` 처리하도록 스위칭 가드를 보강함.
  - `autoFitDemoContainer` 내부의 제목 선택자를 `.slide-title`로 교체하여 실제 슬라이드 제목의 오버플로우를 감지하고 폰트 크기가 자동 축소되도록 수정함.
  - `switchLayout` 및 `updateFont` 함수가 실행되는 시점에도 `autoFitDemoContainer`를 다시 구동하여 화면 구성이나 폰트 설정이 바뀌는 즉시 폰트 크기가 실시간으로 재피팅되도록 트리거를 추가함.

## 📌 [2026-06-18] 레이아웃 종류에 따른 고유 화면 구성(제목 구역, 하단 요약바, 카드 테마 온/오프) 동적 분리 구현 (13차 보완)
- **현상 및 문제**:
  - 기존 데모 시뮬레이터에서는 모든 레이아웃에 대해 획일적으로 상단 제목(`slide-header`) 및 하단 요약바(`.bottom-takeaway-bar`)가 고정되어 표시되었고, 모든 콘텐츠가 콘텐츠 박스(`.content-box`) 테마 스타일(카드 배경, 테두리, 패딩 등) 안에 들어가 렌더링되었음.
  - 이로 인해 실제 스킬의 템플릿(예: 표지형 `hero`, 마무리형 `closing`, 인용형 `quote` 등)처럼 상/하단 바가 없고 화면 전체에 카드 디자인 없이 꽉 찬 비율로 연출되는 고유 구성을 점검할 수 없는 결함이 발생함.
- **해결 및 개선 패턴**:
  - `switchLayout` 자바스크립트 함수 내에서 `title`, `hero`, `closing`, `quote`, `fullbleed`를 `center-layout` 계열로 지정하여, 해당 레이아웃 렌더링 시 상단 제목과 하단 요약바를 동적으로 숨기도록(`display: none`) 처리함.
  - 콘텐츠 카드 박스(`.content-box`)에 대해 `center-layout-active` 전용 CSS 룰을 추가하여, 전체화면/중앙정렬형 레이아웃 실행 시 카드 디자인(배경색, 테두리, 그림자, 블러, 패딩 및 여백)을 강제 무력화하여 투명하고 꽉 찬 화면을 렌더링하도록 `updateCardPreset` 로직을 분기 및 개선함.

## 📌 [2026-06-18] 데모 시뮬레이터 레이아웃 탭 3열(3x7)에서 7열(7x3) 그리드로 개편 (12차 보완)
- **현상 및 필요성**:
  - 기존 3열 배치로 인해 한 화면에 세로로 길게 늘어지던 19종 레이아웃 탭 목록을 가로로 더 넓게 활용하여 직관적으로 볼 수 있도록 7열 배치 구조로 레이아웃 개편 요청됨.
- **해결 및 개선 패턴**:
  - `theme_demo.html` 파일의 `.preview-tabs` 그리드 스타일(`grid-template-columns`)을 `repeat(3, 1fr)`에서 `repeat(7, 1fr)`로 전면 수정하여 가로 폭을 최적화해 7열 그리드 정렬을 보장함.

## 📌 [2026-06-18] 데모 시뮬레이터 19종 레이아웃 탭 전환 차단 버그 해결 및 3열 그리드 탭 개편 (11차 보완)
- **현상 및 문제**:
  - `update_demo_v2.py` 파이썬 자동화 스크립트 실행 시 정규표현식 매칭 실패로 인하여 `theme_demo.html`의 탭 마크업(7종 탭 버튼)과 자바스크립트 스위칭 로직이 교체되지 못하고 7개 레이아웃 버튼만 그대로 노출되는 치명적인 누락 현상이 발생함.
- **해결 및 개선 패턴**:
  - `theme_demo.html` 파일의 마크업 내 7종 탭 버튼을 19종 전체 레이아웃 탭 버튼으로 직접 교체함.
  - 사용자 편의를 위해 탭 메뉴 영역(`.preview-tabs`)을 3열 그리드(`grid-template-columns: repeat(3, 1fr)`) 스타일로 전면 개편하여 모바일 및 데스크톱 가독성 확보.
  - 슬라이드 1, 2, 3 각각의 카드 본문(`.content-box` 내부)에 12종 추가 레이아웃 마크업을 데이터 밸런스에 맞춰 수동 삽입하여 각 레이아웃 구동 시 실전 같은 텍스트 침범 여부 점검 가능하도록 구성.
  - 자바스크립트의 `switchLayout` 함수 내부 `layoutMap` 및 `selectorMap`을 19종에 맞추어 완전 정합.

## 📌 [2026-06-18] 데모 시뮬레이터 19종 레이아웃 완전 확장 및 시각 효과 극대화 (10차 보완)
- **현상 및 문제**:
  - 기존 7종의 레이아웃만 데모에 적용되어 있어, 나머지 12종 템플릿(표지, 분할, 다이어그램, 인용구 등)에 대한 실시간 테마 시뮬레이션 및 검증이 불가능함.
  - 버블원형(`decor-circle`) 장식이 투명도가 낮고 모서리에 위치하여 거의 눈에 띄지 않음.
  - 화면 전환 모션 옵션들이 단순히 속도/타이밍 변화에 머물러 체감 차별성이 부족함.
- **해결 및 개선 패턴**:
  - `templates\base.html`에 파편화되어 있던 나머지 12종 레이아웃의 마크업 및 CSS를 `theme_demo.html`로 전부 이식하여, 19종 전체 레이아웃 구동 환경을 통합 구축.
  - 사용자의 빠른 레이아웃 전환을 위해 상단 선택 메뉴를 3열 버튼 그리드(Grid) 레이아웃으로 직관적으로 개편.
  - 버블원형 장식의 투명도를 올리고 블러(80px)를 극대화하여 배경 캔버스를 물들이는 몽환적인 스크린(Screen) 블렌딩 광원 무드로 개편해 가시성 대폭 상향.
  - 모션 전환(`transition`) CSS에 각각의 모션 클래스별로 차별화된 3D Perspective, 모션 Blur(스캔 효과), 바운스 Scale 효과 등을 배정하여 역동성을 끌어올림.

## 📌 [2026-06-18] 파이썬 스크립트 정규식 치환 시 오탈자 찌꺼기(}%)`;) 해결 및 7종 전체 레이아웃 검증 (9차 보완)
- **현상 및 문제**: 
  - `update_demo_html.py` 스크립트를 사용하여 HTML과 JavaScript 코드를 자동 치환하는 과정에서, `updateSlidePosition` 함수의 `translateX` 문자열 치환 시 `}%)`;` 찌꺼기 문자열이 뒤에 오탈자로 추가 주입되는 문법적 버그 발생.
  - 이로 인해 브라우저 로딩 시 JavaScript 구문 분석 에러(SyntaxError)가 발생하여 데모 스크립트 전체가 작동하지 않는 치명적 문제 발생.
- **해결 및 개선 패턴**:
  - `theme_demo.html` 파일 내부의 `updateSlidePosition` 함수에서 깨진 찌꺼기 코드(`}%)`;` 및 중복 선언문)를 수동 정리하고 정상 브레이스(`}`) 닫기를 수행하여 구문 오류 완전 해결.
  - 이를 통해 7종 모든 레이아웃(목록형, 통계형, 퀴즈형, O/X대비형, 매트릭스형, 로드맵형, 실습형)에 대해 컬러, 카드 프리셋, 아이콘, 폰트, 강조선, 모션(전환 애니메이션)이 실시간으로 교차 튜닝 및 렌더링 검수되도록 보장.

## 📌 [2026-06-18] 동적 테마 3대 추가 아이디어 및 다중 슬라이더와 독립 조율 기능 구현 (8차 보완)
- **현상 및 필요성**:
  1. **테마별 시각 개성 한계**: 동적 테마가 컬러와 카드, 패턴 변경에만 머무르면 텍스트 강조(`<strong>`) 및 띠 배너가 획일적이라 테마별 개성이 100% 살지 않는 비주얼 아쉬움이 있음.
  2. **동적 모션 체감 불가**: 데모가 단일 슬라이드 구조라 테마별 고유의 화면 전환 애니메이션(Motion Transition)을 체감할 수 없으며, 강조 스타일과 하단바 스타일이 독립적인 셀렉트 박스로 분리되지 않아 개별 튜닝 조작이 불가능함.
- **해결 및 구현 기능**:
  1. **강조 텍스트(`<strong>`) 테마 독립 연동**: 강조 텍스트 스타일(`.highlight-*`)을 분리하여 드롭다운 선택 메뉴를 신설하고 테마별 연출 구현 (친근: 형광펜 언더 마커 / 테크: 네온 대괄호 감싸기 / 클래식: 이중 이탤릭 밑줄 / 에디토리얼: 반전 패치 박스 / 칠판: 빨간 펜 구불구불 밑줄).
  2. **하단 핵심 요약 배너(Takeaway Bar) 셰이프 테마 독립 연동**: 일자형 띠에서 테마별 독자적 셰이프(`.takeaway-*`)로 튜닝 가능하도록 드롭다운 신설 (친근: 둥글둥글 구름 라운드 / 테크: HUD 사선 네온 컷 / 클래식: 슬림 보더 & 명조체 / 에디토리얼: 미니멀 라인 구분선 / 칠판: 나무 분필 받침대).
  3. **다중 슬라이더(`.slide-track`) 및 모션 전환 구현**: 데모를 3개 슬라이드 페이지 구조로 확장하고 이전/다음 네비게이션 화살표를 배치. 테마별 화면 전환 템포(Easing과 Duration, 예: 친근 바운스 모션, 사이버 고속 스캔 등)를 슬라이딩 애니메이션으로 완벽히 직접 체험할 수 있도록 고도화 완료.

## 📌 [2026-06-18] 동적 테마 배경 패턴 및 추상기하도형 장식 가시성/가림 현상 해결 (7차 보완)
- **현상 및 문제**:
  1. **배경 패턴(설계도면, 테라초 등) 미노출**: `.pattern-blueprint` 및 `.pattern-terrazzo` 개별 패턴 클래스에 `opacity: 0.08`이 명시되어 있어, 부모 오버레이인 `.pattern-overlay`의 투명도(`0.12`)와 중첩 곱 연산(0.12 * 0.08 = 0.0096)이 적용되면서 1% 미만의 극단적인 불투명도로 인해 화면에 전혀 보이지 않는 현상 발생.
  2. **추상기하도형 장식 가림/쏠림**: 4종의 기하도형(반원, 삼각형, 사각형, 원형) 중 우상단에 위치한 삼각형(`.decor-shape-2`)만 화면 가로 중앙 근처(right: 220px)에 단독으로 노출되고, 나머지 3종은 `z-index: 1` 상태에서 `z-index: 10`인 본문 카드 영역(`.content-box`)과 하단 띠 배너(`.bottom-takeaway-bar`) 밑으로 파묻혀 완전히 가려지는 레이아웃 결함 발생.
- **해결 및 개선 패턴**:
  1. **배경 패턴 투명도 정상화**: `.pattern-blueprint`와 `.pattern-terrazzo`에서 중첩 opacity 연산을 걷어내고, 설계도면(blueprint) 격자는 얇은 선의 시인성을 높이기 위해 `opacity: 0.25`로 상향 지정하여 세련되면서도 명확하게 렌더링되도록 처리.
  2. **장식 레이아웃 및 z-index 상향**: 기하도형의 `z-index`를 카드보다 높은 `11`로 상향하여 본문 카드 코너 경계선 위에 입체적으로 걸칠 수 있도록 개선하고, 투명도를 `0.22`로 보완해 시인성 확보. 또한 슬라이드 모퉁이 여백 및 코너 라인에 맞추어 개별 도형의 좌표와 크기를 살짝 리사이징하여 4개 도형이 모두 조화롭게 시각화되도록 패치.

## 📌 [2026-06-16] 슬라이드 레이아웃 고정 및 AutoFit 격리 설계 (6차 보완)
- **현상 및 문제**: 슬라이드 본문의 텍스트 양에 따라 전체 폰트 크기를 자동 조절(AutoFit)하게 될 때, 제목 구역(`.slide-header`)의 여백이나 하단 바(`.bottom-takeaway-bar`)의 높이 및 글자 크기가 `em` 단위(상대 단위)로 지정되어 있어 슬라이드마다 레이아웃 구역 높이가 널뛰기를 하는 현상 발생. (예: 폰트가 많이 줄어든 슬라이드는 제목과 본문 사이 간격이 좁아 보임)
- **해결 및 개선 패턴**: 
  1. 슬라이드 아키텍처를 '위아래 고정 구역(px 단위)'과 '가운데 가변 구역(`flex: 1`)'으로 완벽히 분리함.
  2. 헤더 구역, 팁 카드 여백, 스텝 배지 크기, 하단 바 높이 및 내부 폰트 크기 등을 모두 `em`에서 절대 단위 `px`로 변경하여 AutoFit의 폰트 축소 마법으로부터 완전히 격리. 
  3. 결과적으로 AutoFit은 가운데 본문 구역(`slide-body`) 내부의 글씨들에만 영향을 미치며 전체 프레임 레이아웃은 단 1픽셀도 변하지 않게 확립함.

## 📌 [2026-06-16] 템플릿 엔진 정규식 수량자(Quantifier) 붕괴 및 웹 폰트 로드 타이밍(Auto-Fit) 누락 해결 (5차 보완)
- **현상 및 문제**: 
  1. `build_html.py`에서 불필요한 `{{#if}}` 찌꺼기를 일괄 삭제하는 정규식(`r'{{#if [A-Z_]+}}.*?{{/if}}'`)을 작성하였으나, 중괄호(`{}`)가 정규식 엔진에서 수량자 기호로 뺏겨버려 매칭에 실패하고 화면에 고스란히 유출되는 치명적 버그 발생. (더불어 숫자형 데이터 치환 누락 버그 동반 발생)
  2. 비동기 이미지 로드(`onload`) 시점에 `autoFitContainer`를 구동하는 가드를 만들었으나, 이미지가 캐싱되어 `complete` 상태로 바로 넘어가버리거나 무거운 **웹 폰트(Noto Sans KR)가 로딩 완료되기 직전**에 폰트 피팅이 실행되어, 폰트 적용 직후 글씨가 불어나서 하단 팁 카드가 영역 밖으로 잘려 나가는 현상(기하학적 겹침 감지 우회) 발생.
- **해결 및 개선 패턴**:
  1. **정규식 중괄호 이스케이프 철저화 & 단순 변환 가드**: 템플릿 엔진에서 정규식으로 중괄호를 타겟팅할 때는 반드시 `r'\{\{\#if ... \}\}'` 처럼 역슬래시로 단단히 감싸 수량자 오작동을 차단. 단순 치환 시에는 값의 타입(`isinstance`)을 묻지 않고 무조건 `str(value)`로 강제 형변환하여 안전하게 `replace` 수행.
  2. **다중 트리거 기반 Auto-Fit 가드 확립**: `document.fonts.ready.then()`을 활용해 웹 폰트 렌더링 직후에 최종 피팅을 재정합시키고, Reveal.js의 `slidechanged` 이벤트에서 비활성(`display:none`)에 의한 기하학적 측정 오차를 방어하기 위해 활성 슬라이드 진입 시 피팅 함수를 강제로 1회 더 구동함.

## 📌 [2026-06-16] 정규식 이스케이프 헬(Escape Hell) 해결 및 비동기 이미지 로드 레이아웃 시프트 완벽 차단 (4차 보완)
- **현상 및 문제**: 
  1. `build_html.py` 템플릿 컴파일러에서 포맷팅과 정규식이 겹칠 때 중괄호 이스케이프(`\{`, `\}`)가 누락되어 `re.sub` 매칭에 실패하고 `{{else}}` 및 플레이스홀더 영역이 지저분하게 화면에 유출되는 버그 발생.
  2. Reveal.js 로딩(`ready` 시점) 이후에 이미지가 비동기적으로 다운로드되면서 뒤늦게 화면 높이가 팽창(Layout Shift)하였고, 이로 인해 좌측의 팁 카드가 찢어지며 하단으로 밀려 잘리는 현상 발생.
- **해결 및 개선 패턴**:
  1. **중괄호 정규식 이스케이프 완비**: f-string의 중괄호 겹침 헬을 탈출하기 위해 문자열 결합식 정규식 패턴(`r'\{\{#if ' + key + r'\}\}...'`)을 적용하여 리터럴 중괄호를 정규식 엔진에 100% 매칭 성공시킴.
  2. **이미지 비동기 로드 이벤트 리스너 도입**: `base.html`에 각 이미지 요소 개별 `onload` 이벤트 감지기(`setupImageLoadListeners`)를 구축. 이미지가 다운로드 완료되는 그 찰나의 순간 해당 슬라이드를 지목해 `autoFitContainer`를 재수행(폰트 크기 재정합)하게 함으로써 잘림 현상을 완벽 차단함. `window.onload` 시점에도 최종 정합 보장.


## 📌 [2026-06-16] 템플릿 컴파일러 내 Handlebars else 분기 연산 긴급 지원 (Hotfix)
- **현상 및 문제**: `layout_split.html` 등에서 이미지 유무에 따른 `{{else}}` 구문 사용 시, 단순 템플릿 치환 파서(`build_html.py`)가 else 분기를 인지하지 못해 화면에 `{{else}}` 텍스트 찌꺼기가 그대로 유출되고, 플레이스홀더 영역이 오작동하거나 붕괴되는 오류 발생.
- **해결 및 개선 패턴**: `build_html.py` 렌더러 내의 조건부 렌더링 정규식 로직에 else 패턴(`pattern_else = rf'{{#if ...}}...{{else}}...{{/if}}'`)과 단순 패턴(`pattern_simple`)을 구분하는 연산 알고리즘을 도입. `val`이 참일 때와 거짓일 때 각각 1그룹(참) 영역과 2그룹(거짓) 영역만 정확히 남기도록 매핑하여 슬라이드 엔진의 조건분기 기능 강화.


## 📌 [2026-06-16] 기하학적 폰트 스케일러 적용 및 템플릿 명암 대비 붕괴 전수 조치 (2차 개선)
- **현상 및 문제**: 
  1. 스텝 슬라이드에서 텍스트 분량이 길어지면 주의사항 카드가 하단 요약 바 밑으로 밀려나 잘림 현상이 재발함. 기존 `scrollHeight` 기반 체크는 flex 구조와 다중 `overflow: hidden`으로 인해 넘침 감지 누락이 발생함.
  2. `quiz` 버튼, `vs_ox` 칼럼, `roadmap` 카드, `closing` 선서문 상자 등의 테두리와 배경색이 동적 테마 가드(`var(--color-card-border/bg)`)를 타지 않고 고정 rgba값으로 하드코딩되어, 밝은 배경 테마일 때 흰색에 묻혀 전혀 분간이 되지 않는 대비 붕괴 발생.
  3. `vs_ox` 본문 리스트 및 `quiz` 버튼 내부 텍스트가 좌측 쏠림으로 렌더링되어 중앙 정렬된 타이틀과 시각 밸런스가 흐트러짐.
  4. `layout_tutorial.html`에 `.tutorial-warning` 마크업은 존재하나 CSS 정의가 누락되어 주의사항이 단순 텍스트로 깨져 렌더링됨.
- **해결 및 개선 패턴**:
  1. **기하학적 겹침 감지(Geometrical Boundary Checker) 도입**: 브라우저의 실제 렌더링 픽셀 좌표(`getBoundingClientRect()`)를 대조해 콘텐츠 마지막 요소가 하단 요약 바의 탑 영역을 넘거나 캔버스 바텀을 이탈할 시 오버플로우로 판정하여 폰트를 축소하도록 알고리즘 개조.
  2. **대비 결함 전수 정합**: 하드코딩된 테두리/배경색을 `var(--color-card-border)` 및 `var(--color-card-bg)`로 전수 전환하여 밝은 배경 시 옅은 회색 구분 테두리와 섀도우가 아름답게 먹히도록 가드 복원.
  3. **대칭 정렬 보강**: `quiz` 버튼과 `vs_ox` 비교 영역의 요소를 `justify-content: center; text-align: center;`로 맞추어 비주얼 정밀도를 높임.
  4. **헤더 옅은 구분선 가이드**: 슬라이드 상단 헤더 하단에 1px 두께의 초박형 은은한 구분선 가이드라인을 삽입해 여백을 보존하면서 정돈감 제공.
  5. **누락 스타일 완비**: `.tutorial-warning` 전용 카드 스타일을 CSS에 추가하여 비주얼 완성도 통일.


## 📌 [2026-06-16] 텍스트 오버플로우 구조 개조 및 이미지 프레임 테마 요소화 (스킬 보완)
- **현상 및 문제**: 
  1. `.split-layout`에 `height: 100%`가 지정되어 있어 상하단 바가 있을 때 높이를 강제 초과하여 하단이 잘림. 이로 인해 폰트 자동 축소 스크립트가 오버플로우를 방어하지 못하는 현상 발생.
  2. `hands_on` 및 `tutorial` 등에서 이미지가 없을 때 이미지 영역이 붕괴되어 우측이 완전히 비어 보이는 결함.
  3. `stats` 통계 레이아웃이 3열 고정(`cols-3`) 그리드여서 2개 이하의 수치 카드 제공 시 우측 쏠림 및 빈 공간 발생.
  4. `closing` 슬라이드 본문(선서문)이 좌측 쏠림으로 렌더링되어 마무리 슬라이드의 정돈감이 깨짐.
- **해결 및 개선 패턴**:
  1. **Flexbox 구조화**: `.split-layout` 높이를 `height: 100%`에서 `flex: 1; min-height: 0;`으로 변경하여 화면 720px 이내로 고정, 오버플로우를 감지하고 폰트 자동 축소 로직이 올바르게 동작하도록 조치.
  2. **이미지 프레임 테마화 및 플레이스홀더**: 이미지가 없어도 이미지 브라우저 프레임은 무조건 렌더링하고, 이미지가 비어있을 때는 공통 SVG 플레이스홀더 UI를 표시. 이미지 프레임을 CSS 변수(`--image-frame-*`)로 분리해 테마 연동 요소로 승격.
  3. **통계 그리드 Flexbox 정렬**: Grid 구조를 Flexbox 중앙 정렬(`display: flex; justify-content: center; flex-wrap: wrap;`)로 바꾸고, 개별 수치 카드에 유연한 너비(max 350px, min 220px)를 부여하여 2개 이하에서도 중앙에 예쁘게 정합되도록 처리.
  4. **마무리 슬라이드 중앙 정렬**: `layout_closing.html` 내부에 `.closing-layout-slide` 전용 불릿 리스트 정중앙 정렬 오버라이딩 스타일을 적용하여 정돈성 확보.


## 📌 [2026-06-16] 동적 테마 가용성 강화 및 레이아웃 바인딩 결함 패치 (스킬 개선)
- **현상 및 문제**: 
  1. 밝은 테마 배경에서 카드가 묻혀 보임 (명도 대비 붕괴).
  2. 하단바 내용이 길어질 때 말줄임(...)으로 잘려 정보 유실.
  3. 에이전트가 변수명 오타(예: `STATS_ITEMS`, `QUIZ_QUESTION`)를 생성하여 렌더링 누락 발생.
  4. 단일 슬라이드에 하나의 튜토리얼 스텝만 툭 던지는 비정상적 기획 흐름 발생.
- **해결 및 개선 패턴**:
  1. **YIQ 명도 자동 판정 가드 구축**: `build_html.py`에 배경색 밝기 연산(`is_light_color`)을 추가하여 밝은 배경 시 카드 불투명도(65%) 및 테두리선(0.12)을 지능적으로 높여 시각적 경계선 확보.
  2. **하단 요약바 래핑 허용**: CSS 수정으로 `white-space: normal`, `height: auto`를 적용하여 가로폭을 넘어가도 자동 줄바꿈으로 정보 유실 방지.
  3. **레이아웃 스키마 강제 명세화**: `design-contract.md`에 각 레이아웃 고유 키(예: `QUESTION`, `STAT_ITEMS`)의 매핑 테이블을 공식 명문화하여 AI의 임의 추측 오타 방지.
  4. **다중 슬라이드 구성 강제 규약**: `SKILL.md`에 스텝퍼 진행률이 동반되는 레이아웃(`tutorial`, `hands_on`) 사용 시 반드시 단계별로 분할된 독립 슬라이드 세트를 연속 생성하도록 명시.
  5. **컴포넌트 디테일 향상**: `layout_closing.html` 폰트 스케일 상향(44px) 및 선서문 노출을 위한 `BULLET_ITEMS` 바인딩 영역 추가, OX 컬럼 세로 중앙 정렬 보완.

## 📌 [2026-06-16] 슬라이드 컴파일러 동적 테마 요소 명세(Theme Styling Interface) 설계

### 1. 배경 및 문제 상황
- 슬라이드 템플릿 엔진이 테마에 반응할 때 단순히 컬러 코드나 텍스트 색상만 변경하면 비주얼적 차별화가 약해 템플릿 느낌을 지우기 힘듦.
- 반면 테마에 따라 레이아웃 구조(그리드, 컴포넌트 크기)를 매번 바꾸면 화면이 깨지거나 예외 처리가 비대해져 안정성이 떨어짐.

### 2. 해결 패턴 (Theme Styling Interface)
- **HTML 구조 및 그리드는 100% 보존**하되, **최상위 테마 클래스**(`theme-cute-note`, `theme-cyber-tech` 등)의 스타일 제어를 받는 **9가지 표준 테마 요소(Theme Styling Interface)**를 규약으로 정의함.

#### 1) 4대 테마 제어 요소 규약
1. **슬라이드 캔버스 배경 (Slide Canvas Background)**: 전체 배경색뿐 아니라, 테마별 연한 보조선 격자 패턴(모눈 격자, 디지털 HUD 격선, 대각 스트라이프 등)을 투명도 8%~15% 수준으로 오버레이하여 깊이감 부여.
2. **콘텐츠 카드/박스 (Content Card/Box)**: 본문을 담는 상자의 테두리 스타일(Dashed/Solid), 모서리 둥글기(Radius), 그림자 깊이(Shadow), 투명도 및 블러(Glassmorphism)를 제어.
3. **타이포그래피 및 강조 (Typography & Highlights)**: 제목/본문 글꼴(Font family)과 텍스트 내 강조 단어(`<strong>` 등)에 씌워지는 시각적 형태(형광펜 효과, 디지털 대괄호 등) 지정.
4. **이정표 영역 (Wayfinding UI)**: 상단 스텝퍼(Stepper), 좌상단 섹션 헤더(Section Header), 하단 핵심 요약 띠(Bottom Takeaway)의 데코레이션 스킨 제어.

#### 2) 아이콘 및 이미지 에셋 공급 이원화 (1, 3번 하이브리드 전략)
- **벡터 아이콘 (Lucide CDN + CSS 스타일링)**: HTML 아이콘 코드는 일치시킨 상태에서 CSS 속성만으로 테마별 렌더링 스위칭.
  - *큐트 테마*: 두꺼운 선(2.5px) + 둥근 파스텔 배경 배지
  - *테크 테마*: 얇은 선(1.2px) + 네온 광채(Glow) + 각진 뷰파인더 꺾쇠 배지
  - *비즈니스*: 표준 선(2.0px) + 라운드 코너(8px) 미니멀 회색 배지
- **설명용 삽화**: 정교한 고품질 일러스트가 필요할 시 `generate_image` 툴을 사용해 배경이 투명한(Transparent PNG) 고품질 일러스트를 실시간 생성해 배치.

---
